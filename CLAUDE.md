# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

このファイルはClaude Codeが毎回読み込むプロジェクト説明書です。

---

## プロジェクト概要

- **名称**: RaiseTimeLine（SNS アプリケーション）
- **目的**: AIコース課題。Twitter/X 風の SNS アプリを通じて、要件定義・設計・実装の一連のプロセスを体験する
- **ユーザー**: 受講生・個人ユーザー（複数ユーザーでの同時利用を想定）
- **ステータス**: 要件定義フェーズ完了。実装フェーズ未着手

---

## ドキュメント構成

要件定義書.md がハブとなり、各詳細ドキュメントへリンクしている。

```
RaiseTimeLine/
├── CLAUDE.md                       ← このファイル
├── 要件定義書.md                    ← ハブ（概要 + 全ドキュメントへのリンク）
├── README.md
└── docs/
    ├── 01_機能要件.md
    ├── 02_非機能要件.md
    ├── 03_バリデーション.md
    ├── 04_ユースケース.md
    ├── 05_画面仕様.md
    ├── 06_データ設計.md
    ├── 07_技術仕様.md
    └── features/                   ← 機能単位の詳細定義
        ├── F01_認証_機能定義書.md
        ├── F02_タイムライン_機能定義書.md
        ├── F03_投稿_機能定義書.md
        ├── F04_コメント_機能定義書.md
        ├── F05_いいね_機能定義書.md
        ├── F06_フォロー_機能定義書.md
        └── F07_画像投稿_機能定義書.md
```

---

## 技術スタック

| 領域 | 技術 | バージョン |
|------|------|-----------|
| フロントエンド | React + TypeScript | React 19.2.6 / TypeScript 6.0.x |
| ルーティング | React Router | 7.x |
| HTTP クライアント | Axios | 1.x |
| フロントビルド | Vite | 8.0.x |
| バックエンド | Java / Spring Boot | Java 21 / Spring Boot 3.5.0 |
| 認証 | Spring Security（JWT） | （Spring Boot 管理） |
| ORM | Spring Data JPA | （Spring Boot 管理） |
| DB | PostgreSQL | 17 |
| バックビルド | Gradle | 8.14.4 |
| 画像ストレージ | AWS S3 | - |

---

## Git / GitHub 運用ルール

### 基本方針

- **main への直接プッシュは禁止**（GitHub 側でブランチ保護を設定済み）
- すべての作業はイシュー起票 → ブランチ作成 → PR の流れで行う
- **現在のブランチが `main` の場合、作業を開始する前に必ず新しいブランチを切る**

### 作業開始の手順（厳守）

1. **GitHub イシューを作成する**（ブランチ作成より先に行う）
2. イシュー番号を確認してブランチを作成する（例: `git checkout -b feature/issue-2-xxx`）
3. 作業・コミット・プッシュ → PR 作成

### イシュー運用

- 作業を始める前に必ず GitHub イシューを作成する（コードに触る前に行う）
- タイトル: 日本語で簡潔に（例: `ログイン API の実装`）
- ラベルを付ける: `feature`（新機能）/ `bug`（不具合）/ `docs`（ドキュメント）/ `chore`（設定・雑務）

### ブランチ命名規則

```
<type>/issue-<番号>-<短い説明>
```

| type | 用途 |
|------|------|
| `feature` | 新機能 |
| `fix` | バグ修正 |
| `docs` | ドキュメント |
| `chore` | 設定・雑務 |

例: `feature/issue-2-login-api`、`docs/issue-5-update-er`

### PR ルール

- タイトル: `[#イシュー番号] 日本語の説明`（例: `[#2] ログイン API の実装`）
- 本文に `Closes #番号` を書いてイシューと紐付ける
- main へのマージは PR 経由のみ（直接プッシュ不可）

### コミットメッセージ

- 日本語で記述する
- 例: `ログインエンドポイントを追加`

### 危険な操作

- `rm` / `git reset --hard` / `git clean` / `git push --force` 等は必ず実行前にユーザーに確認する

---

## サーバー起動ルール

### 起動順序（厳守）

```
① DB（Docker） → ② バックエンド → ③ フロントエンド
```

DB が起動していない状態でバックエンドを起動すると、PostgreSQL 接続エラーで落ちる。

### 各サービス

| 対象 | ディレクトリ | コマンド | URL |
|------|------------|---------|-----|
| DB（PostgreSQL） | プロジェクトルート | `docker compose up -d` | localhost:5432 |
| バックエンド | `backend/` | `./gradlew bootRun` | http://localhost:8080 |
| フロントエンド | `frontend/` | `npm run dev` | http://localhost:5173 |

### ポート競合ルール

- ポートが競合した場合は既存プロセスを**必ず停止**してから起動する
- 別ポートでの代替起動は禁止（フロント↔バックエンド間の接続が壊れるため）
- カスタムコマンド `/start-dev` で詳細手順を確認できる

---

## カスタムコマンド

| コマンド | 用途 |
|---------|------|
| `/start-dev` | DB・バックエンド・フロントエンドを起動 |
| `/commit` | 変更をコミットしてリモートにプッシュ |
| `/pr` | プルリクエストを作成 |
| `/review` | 実装が要件定義・設計と合っているか確認 |
| `/code-review` | コード品質・セキュリティ・保守性の総合レビュー |
| `/quality-check` | ESLint・Checkstyle を実行して品質チェック |

---

## このユーザーとの協働ルール

- **言語**: 日本語で回答する
- **コードコメント**: 原則なし（必要な場合のみ最小限）
- **説明スタイル**: 簡潔に。長い説明よりも短い要点を好む
- **作業確認**: 不可逆な操作（削除・強制プッシュ等）は必ずユーザーに確認してから実行する
- **ドキュメント**: 要件定義書.md をハブとして、詳細は docs/ 配下の各ファイルを参照・更新する
